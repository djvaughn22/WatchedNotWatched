import { WatchCompanion } from '../components/WatchCompanion';

export const metadata = {
  title: 'Watch Companion — WatchedNotWatched',
  description: 'Real-time mute and skip companion for your streaming service.',
};

export default function WatchPage() {
  return <WatchCompanion />;
}
